// Package webmention provides functions for discovering the webmention
// endpoint for URLs, and sending webmentions.
package webmention

const (
	relWebmention  = "webmention"
	relLegacy      = "http://webmention.org"
	relLegacySlash = "http://webmention.org/"
)
